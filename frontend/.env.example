# Frontend environment (Vite exposes only VITE_-prefixed vars)
# The dev server proxies /api → http://localhost:8000 via vite.config.js,
# so in most cases you don't need to set anything here.

# Override the API base URL (defaults to '/api')
VITE_API_BASE_URL=/api
